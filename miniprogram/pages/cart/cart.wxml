<!--pages/cart/cart.wxml-->

<image class="bg" src="/images/bg.png" />

<view wx:if="{{!userInfo}}">
    <login bind:onLogin="onTapLogin"></login>
</view>

<view class="cart-card" wx:else>
    cart-card
</view>


